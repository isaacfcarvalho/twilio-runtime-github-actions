{
  "name": "twilio-ci-test",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run --env"
  },
  "dependencies": {
    "@types/qs": "6.9.4",
    "@types/express": "^4.17.7"
  },
  "devDependencies": {
    "twilio-run": "^2.0.0"
  },
  "engines": {
    "node" : ">=8.0.0 <11.0.0" 
  }
}
